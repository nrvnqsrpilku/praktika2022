import math

def factorial_cycle():
    sum = 0
    e = 0.001
    x = 1
    k = 1
    i = 13
    factorial = k * 1
    while k <= i:
        sum += math.pow(-1, 1) * ((math.sin(math.pow(x,2))*math.cos(math.pow(x,3)) - math.sin(x) + 5.2) * math.pow(x, 1))
        print("{:.3f}".format(sum))
    print("{:.3f}".format(sum))

factorial_cycle()
int()
